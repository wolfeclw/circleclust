

### slist NA funs

mean_nag <- function(x) {mean(x, na.rm = TRUE)}
sd_nag <- function(x) {sd(x, na.rm = TRUE)}
median_nag = function(x) {median(x, na.rum = TRUE)}
min_nag = function(x) {min(x, na.rm = TRUE)}
max_nag = function(x) {max(x, na.rm = TRUE)}
